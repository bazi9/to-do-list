document.addEventListener('DOMContentLoaded', function() {
    // Automatically focus on the main task input field
    const taskInput = document.getElementById('task');
    if (taskInput) {
        taskInput.focus();
    }
});
